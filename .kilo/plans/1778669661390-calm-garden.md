# CorteQS Diaspora Globe - Durum Analizi ve Kalan Plan

## YAPILANLAR (Tamamlanan TODO'lar)

### Faz 1 — Proje İskeleti ✅
- **TODO 001**: Vite + React + TypeScript projesi kuruldu
- **TODO 002**: Klasör yapısı oluşturuldu (`src/app/`, `src/lib/`, `src/features/`, `src/components/ui/`, `src/styles/`)
- **TODO 003**: Global CSS bağlandı (`src/styles/globals.css`)
- **TODO 004**: App shell oluşturuldu (`src/app/App.tsx`)
- **TODO 005**: Routing altyapısı eklendi (`router.tsx`, `routes.ts`)

### Faz 2 — Environment ve Supabase Client ✅
- **TODO 006**: Env parser oluşturuldu (`src/lib/env.ts`, `.env.example`)
- **TODO 007**: Supabase client oluşturuldu (`src/lib/supabase.ts`)
- **TODO 008**: Database types placeholder eklendi (`src/lib/database.types.ts`)

### Faz 3 — Supabase Migration ✅
- **TODO 009**: Supabase klasörü başlatıldı (`supabase/migrations/`)
- **TODO 010**: Base migration dosyası eklendi (`001_base.sql`)
- **TODO 011**: RLS smoke test (manuel doğrulama gerekli)
- **TODO 012**: Type generation yapıldı (`database.types.ts`)

### Faz 4 — UI Componentleri ✅
- **TODO 013**: Button (`src/components/ui/Button.tsx`)
- **TODO 014**: Input (`src/components/ui/Input.tsx`)
- **TODO 015**: Select (`src/components/ui/Select.tsx`)
- **TODO 016**: Modal (`src/components/ui/Modal.tsx`)
- **TODO 017**: Badge, Spinner, EmptyState, ErrorBox

### Faz 5 — Auth ✅
- **TODO 018**: Session hook (`useSession.ts`)
- **TODO 019**: LoginPage + authApi (magic link)
- **TODO 020**: AuthGate (private route koruması)
- **TODO 021**: Header login/logout durumu

### Faz 6 — Places Frontend ✅
- **TODO 022**: Places types (`types.ts`)
- **TODO 023**: Places frontend API wrapper (`api.ts`)
- **TODO 024**: Debounced autocomplete hook (`usePlaceAutocomplete.ts`)
- **TODO 025**: Place details hook (`usePlaceDetails.ts`)
- **TODO 026**: PlaceSuggestionList component
- **TODO 027**: PlaceAutocomplete component (keyboard navigation dahil)

### Faz 7 — Edge Function Ortak Altyapı ✅
- **TODO 028**: CORS helper (`_shared/cors.ts`)
- **TODO 029**: JSON response helper (`_shared/json.ts`)
- **TODO 030**: Auth helper (`_shared/auth.ts`) — requireUser, requireAdmin
- **TODO 031**: Edge validator helper (`_shared/validators.ts`)
- **TODO 032**: Google Places client (`_shared/googlePlaces.ts`)
- **TODO 033**: Place normalizer (`_shared/normalizePlace.ts`)

### Faz 8 — Places Edge Functions ✅
- **TODO 034**: `places-autocomplete` function
- **TODO 035**: `place-details` function
- **TODO 036**: Function smoke test (manuel)

### Faz 9 — Pin Submit Backend ✅
- **TODO 037**: `pins-submit` function (auth + validation + Google details + normalize + insert + moderation event)
- **TODO 038**: Error mapping
- **TODO 039**: Manuel test (manuel)

### Faz 10 — Pins Frontend ✅
- **TODO 040**: Pin types (`pins/types.ts`)
- **TODO 041**: Pins API wrapper (`pins/api.ts`)
- **TODO 042**: PinStatusBadge
- **TODO 043**: PinForm (autocomplete dahil)
- **TODO 044**: AddPinModal
- **TODO 045**: MyPinsList
- **TODO 046**: MyPinsPage (AuthGate korumalı)

### Faz 11 — Globe Frontend (Kısmen)
- **TODO 047**: ✅ Globe types (`globeTypes.ts`)
- **TODO 048**: ✅ Geometry helper (`geometry.ts`)
- **TODO 049**: ✅ Approved pins hook (`useApprovedPins.ts`) — realtime dahil
- **TODO 050-051I**: ✅ Three.js globe tam implementasyonu (GlobeScene.tsx)
  - Earth sphere + texture loading + fallback
  - Cloud layer
  - Atmosphere glow
  - Dark background
  - Camera setup
  - Lighting (ambient + directional)
  - Rotation + interaction (mouse/touch/zoom)
- **TODO 051J**: ✅ Pin görsel stil (Three.js mesh dot + glow)
- **TODO 051K**: ✅ Globe loading/empty state (GlobePage.tsx)
- **TODO 054**: ✅ Globe controller hook (`useGlobeController.ts`) — flyToCoords
- **TODO 055**: ✅ GlobePage oluşturuldu
- **TODO 056**: ✅ Realtime refetch (useApprovedPins.ts içinde)

### Faz 12 — Admin Backend ✅
- **TODO 057**: ✅ `admin-pins` function (GET, status filter, cursor pagination)
- **TODO 058**: ✅ `admin-pins-review` function (approve/reject + audit event)

### Faz 13 — Admin Frontend ✅
- **TODO 060**: ✅ Admin API wrapper (`adminApi.ts`)
- **TODO 061**: ✅ AdminRouteGuard
- **TODO 062**: ✅ AdminPinsTable
- **TODO 063**: ✅ ReviewDrawer
- **TODO 064**: ✅ AdminPinsPage

### Faz 14 — Validation ve Error UX ✅
- **TODO 065**: ✅ Frontend validators (`src/lib/validators.ts`)
- **TODO 066**: ✅ Error mapper (`src/lib/errors.ts`) — Türkçe mesajlar
- **TODO 067**: ✅ Loading/empty/error standardize

### Faz 16 (Kısmen)
- **TODO 076**: ✅ Build scripts (dev/build/preview/test/test:e2e)

---

## YAPILACAKLAR (Kalan TODO'lar ve Eksikler)

### 1. Globe HTML Overlay Pin Sistemi (TODO 052, 053) — Öncelik: YÜKSEK

**Sorun**: Mevcut implementasyonda pinler Three.js mesh olarak render ediliyor. Spec, HTML overlay pin yaklaşımını öngörüyor. Mevcut durumda:
- Pin hover/tooltip yok
- Arkadaki pinler gizlenmiyor (back-face culling)
- Pin tıklanabilir değil
- Pin bilgi kartı yok

**Dosyalar**:
- `src/features/globe/GlobeOverlayLayer.tsx` — YENİ
- `src/features/globe/GlobePin.tsx` — YENİ

**Adımlar**:
1. `GlobePin.tsx`: HTML tabanlı pin component — glowing dot + hover tooltip
2. `GlobeOverlayLayer.tsx`: Pin listesi al, her pin için 3D→2D projection hesapla, arka yüz pinleri gizle
3. `GlobeScene.tsx`'den Three.js pin mesh'leri kaldır, yerine overlay layer ekle
4. Her frame'de projection güncelle (globe döndükçe pin pozisyonları güncellensin)
5. Tooltip: display name, pin type, city, country

### 2. Admin Review Transaction Safety (TODO 059) — Öncelik: ORTA

**Sorun**: `admin-pins-review` function'ında pin update ve moderation event insert ayrı çağrılar. Yarım kalma riski var.

**Dosyalar**:
- `supabase/migrations/004_admin_review_rpc.sql` — YENİ
- `supabase/functions/admin-pins-review/index.ts` — GÜNCELLENECEK

**Adımlar**:
1. SQL RPC function yaz: `admin_review_pin(p_pin_id, p_action, p_reason, p_admin_id)` — tek transaction içinde pin update + event insert
2. Edge Function'ı RPC çağıracak şekilde güncelle

### 3. Rate Limiting Helper (Spec'te dosya var, TODO yok) — Öncelik: ORTA

**Dosyalar**:
- `supabase/functions/_shared/rateLimit.ts` — YENİ

**Adımlar**:
1. Basit in-memory rate limiter yaz (Deno Deploy uyumlu)
2. Places autocomplete: 30 req/dk/IP
3. Place details: 15 req/dk/IP
4. Pins submit: 10 req/saat/user
5. Admin review: 120 req/saat/admin
6. Edge function'lara entegre et

### 4. Unit Testler (TODO 069, 070, 071) — Öncelik: YÜKSEK

**Eksik test dosyaları**:
- `tests/unit/normalizePlace.test.ts` — YENİ
- `tests/unit/geometry.test.ts` — YENİ
- `tests/unit/validators.test.ts` — YENİ

**Adımlar**:
1. **normalizePlace testleri**: locality, postal_town, admin_level_3 fallback; country missing error; location missing error
2. **geometry testleri**: latLngTo3D valid output; radius etkisi; targetRotationFromLatLng
3. **validators testleri**: displayName min/max; description max; pinType valid/invalid

**Not**: `normalizePlace.ts` Supabase Edge Function ortamında (Deno), testler ise Vitest (Node) çalışıyor. Normalizer logic'i frontend'e de kopyalamak veya test edilebilir modül olarak paylaşmak gerekebilir.

### 5. Playwright E2E Test Altyapısı (TODO 072-075) — Öncelik: ORTA

**Eksik**:
- `playwright.config.ts` — YENİ
- Playwright dependency (package.json'da yok)
- `tests/e2e/public-globe.spec.ts` — YENİ
- `tests/e2e/auth.spec.ts` — YENİ
- `tests/e2e/admin-review.spec.ts` — YENİ
- `tests/e2e/globe-visual.spec.ts` — YENİ (TODO 051L)

**Adımlar**:
1. Playwright kur: `npm install -D @playwright/test`
2. `playwright.config.ts` oluştur
3. Public globe smoke test: `/` açılıyor, globe container görünür, koyu background
4. Auth smoke test: `/login` açılıyor, email input + submit button
5. Admin forbidden test: login olmadan `/admin/pins` → login veya forbidden
6. Globe visual smoke test: canvas render, dark background class

### 6. Deployment Hazırlığı (TODO 078, 079) — Öncelik: DÜŞÜK

**Eksik**:
- `SECURITY_CHECKLIST.md` — YENİ
- `README.md` güncellemesi

**Adımlar**:
1. Security checklist: Non-negotiable kurallar, Google key check, RLS check, admin role check
2. README: Local setup, env açıklaması, Supabase komutları, test komutları, deploy notu

### 7. Son Sertleştirme Kontrolleri (TODO 080-085) — Öncelik: DOĞRULAMA

Bu adımlar kod yazmayı değil, doğrulama içeriyor:
- **TODO 080**: Build çıktısında secret araması
- **TODO 081**: Public feed pending pin dönmüyor mu
- **TODO 082**: Admin action audit log kontrolü
- **TODO 083**: Google request maliyet kontrolü (min 3 char, debounce, field mask)
- **TODO 084**: Full happy path manuel test
- **TODO 085**: Full rejection path manuel test

---

## ÖNERİLEN UYGULAMA SIRASI

| Sıra | İş | Tahmini Süre |
|------|------|-------------|
| 1 | Globe HTML Overlay Pin sistemi (TODO 052, 053) | Büyük adım |
| 2 | Unit testler (TODO 069, 070, 071) | Orta |
| 3 | Admin review RPC (TODO 059) | Küçük |
| 4 | Rate limiting helper | Orta |
| 5 | Playwright kurulumu + E2E testler (TODO 072-075, 051L) | Büyük adım |
| 6 | Security checklist + README (TODO 078, 079) | Küçük |
| 7 | Final doğrulamalar (TODO 080-085) | Manuel |

---

## BİLGİ NOTLARI

- `supabase/config.toml` mevcut değil — `npx supabase init` ile oluşturulabilir
- `public/textures/earth/` klasöründe sadece `.gitkeep` var — texture dosyaları manuel eklenmeli
- Mevcut `flyTo.ts` dosyası yok — flyTo mantığı `useGlobeController.ts` içinde
- `placeUiState.ts` (spec'te listeli) oluşturulmamış — muhtemelen gereksiz bulundu
